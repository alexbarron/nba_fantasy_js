<%= form_for @player, html: {id: "player_form" } do |f| %>
  <%= f.label :player_url, "Player Profile URL" %>
  <%= f.text_field :player_url, id: "playerUrl" %>
  <%= f.submit class: 'btn btn-success' %>
<% end %>